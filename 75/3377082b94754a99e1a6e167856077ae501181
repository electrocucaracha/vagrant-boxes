{"category": "Changed", "changelog_entry": "The GitHub Actions linter workflow has been updated to provide a more consistent and accessible log URL for artifacts, which now points directly to the workflow run page, and to handle jq failures when extracting the super-linter version, preventing unexpected exits by setting the version as \"unknown\" if jq fails.", "summary": "The GitHub Actions linter workflow has been updated to improve artifact logging and error handling. The log URL now points directly to the workflow run page, providing a more consistent and accessible reference. Additionally, the script now handles jq failures when extracting the super-linter version, preventing unexpected exits by setting the version as \"unknown\" if jq fails.", "version": 1}
