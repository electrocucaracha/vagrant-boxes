{"category": "Added", "changelog_entry": "Enabled automated code duplication detection by introducing a .jscpd.json configuration file with a zero duplication threshold, excluding commonly ignored files and directories from scanning to minimize noise and false positives.", "summary": "Code duplication checks are now enforced through a .jscpd.json configuration file with a zero duplication threshold. This setup excludes commonly ignored files and directories from scanning to reduce noise and false positives. The foundation is established for automated code duplication detection, but no immediate migration or breaking behavior is introduced.", "version": 1}
