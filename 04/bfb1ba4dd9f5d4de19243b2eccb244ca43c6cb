{"category": "Changed", "changelog_entry": "Improved the robustness of SSH host key regeneration on first boot by enabling retry logic to handle transient errors encountered during reconfiguration and restart.", "summary": "SSH host key regeneration on first boot is now more robust due to the addition of retry logic. The cron job for regeneration now wraps reconfiguration and restart commands in a loop that retries up to five times, logging failures to /var/log/ssh-host-keys-reconfigure.log. If all retries fail, the script exits with an error code. This improves the reliability of SSH host key regeneration on supported Ubuntu images.", "version": 1}
