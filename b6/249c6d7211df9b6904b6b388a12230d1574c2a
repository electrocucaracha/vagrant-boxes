{"category": "Fixed", "changelog_entry": "The UTM build process has been stabilized by enabling the use of official cloud images and injecting root login settings through the cloud-init seed, which now consumes the UTM-specific NoCloud data as cloud-init seed media.", "summary": "The UTM build process has been refactored to use cloud-init seeds instead of ISO installs. This change affects the Ubuntu 22.04 and 24.04 UTM builds, which now use official cloud images and inject root login settings through the cloud-init seed. The UTM-specific NoCloud data is still used, but it is now consumed as cloud-init seed media, including a dedicated network-config file for the cloud-image workflow.", "version": 1}
