{"category": "Added", "changelog_entry": "Enabled automatic box versioning for Ubuntu base boxes by removing the requirement for a VERSION variable and instead determining the box version based on the Ubuntu release.", "summary": "The build process for Ubuntu Vagrant base boxes has been improved. The `build.sh` script now cleans up conflicting VirtualBox and libvirt guests before validation. Additionally, the `metadata.json` files for local publishing or hosted distribution now use Ubuntu codenames instead of version numbers. The `VERSION` variable is no longer required and the box version is determined automatically based on the Ubuntu release. The `BOX_NAMESPACE` variable has been updated to use `electrocucaracha-boxes` by default.", "version": 1}
