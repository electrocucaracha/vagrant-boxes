{"category": "Changed", "changelog_entry": "Optimized code quality checks by ensuring a clean state for each run through the removal of stale dependencies and virtual environments.", "summary": "The Makefile has been refactored to include a cleanup step in the lint and fmt targets. This change removes stale dependencies and virtual environments before running lint or fmt, ensuring a clean state for each run and improving reproducibility. The cleanup target removes node_modules and Python environments, addressing inconsistent results across environments due to these factors. This change reduces the chance of false positives or negatives during code quality checks.", "version": 1}
