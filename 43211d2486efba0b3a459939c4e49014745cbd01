{"category": "Added", "changelog_entry": "Enabled support for building Vagrant boxes on arm64 architecture with the addition of a new macOS-specific provider.", "summary": "This change adds support for the UTM provider to the Vagrant box builder. The UTM provider is a macOS-specific provider that allows building boxes for arm64 architecture. The build process now includes a new Packer template for UTM, and the `build.sh` script has been updated to handle UTM builds. Additionally, the `PROVIDERS` environment variable now supports the `utm` value, and the `UTM_PACKER_PLUGIN_SOURCE` and `UTM_PACKER_PLUGIN_VERSION` environment variables have been added to specify the Packer plugin source and version for UTM builds.", "version": 1}
