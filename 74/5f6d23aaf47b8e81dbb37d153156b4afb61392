{"category": "Changed", "changelog_entry": "Optimized the linter workflow output size to limit changed files to 80, log files to 40, and error excerpt lines to 80 with a 4000-byte cap, while also removing the raw log excerpt section and improving commit diff excerpt formatting.", "summary": "The linter workflow output size has been reduced to improve readability and performance within GitHub's limits. Key changes include limiting changed files to 80, log files to 40, and error excerpt lines to 80 with a 4000-byte cap. The commit diff excerpt is now limited to 4000 bytes and uses unified=1 for brevity, and the raw log excerpt section has been removed.", "version": 1}
