{"category": "Fixed", "changelog_entry": "The Makefile now checks for the presence of textlint and prettier before use, ensuring consistent formatting regardless of setup, and installs these tools locally if missing to prevent failures in clean environments and reduce setup friction for developers.", "summary": "The Makefile now checks for the presence of textlint and prettier before use, ensuring consistent formatting regardless of setup. This change prevents failures in clean environments and reduces setup friction for developers. The fmt target now installs these tools locally if missing, and uses --no-install with npx to prevent redundant package downloads.", "version": 1}
