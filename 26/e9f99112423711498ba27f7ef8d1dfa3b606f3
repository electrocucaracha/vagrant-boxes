{"category": "Fixed", "changelog_entry": "The GitHub Actions workflow for linter logs is now more robust against future changes to GitHub's artifact URL formats due to the use of upload-artifact output for log URL generation.", "summary": "The GitHub Actions workflow for linter logs now uses the output from the `upload-artifact` action to generate log URLs, making the workflow more robust against future changes to GitHub's artifact URL formats. This change also introduces an explicit upload step ID and passes the artifact URL through an environment variable for later steps. The workflow's behavior remains unchanged, but it is now more resilient to potential future changes.", "version": 1}
