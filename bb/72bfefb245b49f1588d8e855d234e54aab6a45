{"category": "Added", "changelog_entry": "Enabled optional deployment to `/var/www` by introducing a new environment variable `DEPLOY_WWW` with a default value of `false`, allowing users to customize their deployment workflow.", "summary": "Deployment to `/var/www` is now optional. A new environment variable `DEPLOY_WWW` controls this behavior, and its default value is `false`. When set to `true`, the build script copies the published artifacts into the specified `WWW_ROOT` directory after completion. This change allows users to customize their deployment workflow.", "version": 1}
